{
  "name": "tu-proyecto",
  "version": "0.1.0",
  "homepage": "https://kingcordoba.github.io/plan-maestro-financiero",
  "private": true,
  "dependencies": {
    "react": "^18.0.0",
    "react-dom": "^18.0.0",
    "react-scripts": "^5.0.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  },
  "eslintConfig": {
    "extends": [
      "react-app"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last tier 1 chrome versions",
      "last 1 firefox versions",
      "last 1 safari versions"
    ]
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}
